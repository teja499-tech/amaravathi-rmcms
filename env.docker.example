# Django settings
DEBUG=False
SECRET_KEY=your-secure-secret-key-here

# Database settings
DB_NAME=amaravathi_db
DB_USER=amaravathi_user
DB_PASSWORD=your-secure-db-password-here

# Domain settings
DOMAIN=your-domain.com

# Allowed hosts
ALLOWED_HOSTS=${DOMAIN},www.${DOMAIN}

# Supabase settings (if used)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_BUCKET=

# Email settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True 